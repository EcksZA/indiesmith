<%= form_for [@store, @product] do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :price %>
	<%= f.text_field :price %>

	<%= f.label :description %>
	<%= f.text_field :description %>

	<%= f.hidden_field :store_id, value: @store.id %>

	<%= f.submit %>
<% end %>